import GrCascaderPanel from './src/cascader-panel.vue';
/* istanbul ignore next */
GrCascaderPanel.install = function (Vue) {
  Vue.component(GrCascaderPanel.name, GrCascaderPanel);
};

export default GrCascaderPanel;
