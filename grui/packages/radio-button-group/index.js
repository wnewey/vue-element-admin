import GrRadioButtonGroup from './src/radio-button-group';

/* istanbul ignore next */
GrRadioButtonGroup.install = function (Vue) {
  Vue.component(GrRadioButtonGroup.name, GrRadioButtonGroup);
};

export default GrRadioButtonGroup;
